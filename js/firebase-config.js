/**
 * firebase-config.js
 *
 * !! IMPORTANT !!
 * Replace the placeholder values below with your real Firebase project config.
 * You get this from the Firebase console:
 *   https://console.firebase.google.com/
 *   → Your Project → Project Settings → "Your apps" → Web app → SDK setup & config
 *
 * The app will NOT work until these values are filled in correctly.
 * See FIREBASE-SETUP.md for full setup instructions.
 */

const FIREBASE_CONFIG = {
  apiKey:            "YOUR_API_KEY",
  authDomain:        "YOUR_PROJECT_ID.firebaseapp.com",
  projectId:         "YOUR_PROJECT_ID",
  storageBucket:     "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId:             "YOUR_APP_ID",
};
